@model IEnumerable<Projekcik.Entities.Team>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.IdRobota)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.NazwaDruzyny)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.IdRobota)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.NazwaDruzyny)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdDruzyny">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdDruzyny">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdDruzyny">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
